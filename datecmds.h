#ifndef TECHOS_DATEH
#define TECHOS_DATEH

#include "osstate.h"
#include "command.h"

/* Header for date related commands. */
DECLCOM(date);
DECLCOM(datefmt);
DECLCOM(setdate);
DECLCOM(time);

#endif

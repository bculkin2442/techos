#ifndef TECHOS_FILECMDS
#define TECHOS_FILECMDS

#include "osstate.h"
#include "command.h"

/* Header for pcb related commands. */
DECLCOM(ls);
DECLCOM(cd);
DECLCOM(mkdir);
DECLCOM(rmdir);
DECLCOM(touch);
DECLCOM(rm);

#endif

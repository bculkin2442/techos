#ifndef TECHOS_PCBCMDS
#define TECHOS_PCBCMDS

#include "osstate.h"
#include "command.h"

/* Header for pcb related commands. */
DECLCOM(mkpcb);
DECLCOM(rmpcb);
DECLCOM(sspcb);
DECLCOM(rspcb);
DECLCOM(sppcb);
DECLCOM(shpcb);

#endif

#ifndef TECHOS_USERCMDS
#define TECHOS_USERCMDS

#include "osstate.h"
#include "command.h"

/* Header for file related commands. */
DECLCOM(mkusr);
DECLCOM(rmusr);
DECLCOM(pwd);
DECLCOM(mkadm);
DECLCOM(rmadm);

#endif
